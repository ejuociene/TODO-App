import React from 'react';

const Completed = () => {
	// const { list, setRefresh } = useContext(MainContext);
	// const [ message, setMessage ] = useState('');
	// // const saveNewTask = (e) => {
	// // 	e.preventDefault();
	// // 	axios.post('/api/todos/new', formData).then((resp) => {
	// // 		setRefresh((prevStatus) => !prevStatus);
	// // 		setAddNew(false);
	// // 	});
	// // };
	// return (
	// 	<div className="container">
	// 		{/* {message && <div className="message">{message}</div>} */}
	// 		<div className="heading">
	// 			<h1 className="title">ToDo List:</h1>
	// 			<p className="add" onClick={() => setAddNew(true)}>
	// 				+ New
	// 			</p>
	// 		</div>
	// 		<div className="task--list" />
	// 		<div>
	// 			{list.length > 0 &&
	// 				list.map((task) => {
	// 					return <Task task={task} key={task.id} setRefresh={setRefresh} setMessage={setMessage} />;
	// 				})}
	// 		</div>
	// 	</div>
	// );
};

export default Completed;
